# FinGAT 评价指标深度分析

## 📊 评价指标概览

FinGAT使用多任务学习框架，对应5个关键评价指标：

| 指标 | 类型 | 范围 | 目标 | 用途 |
|------|------|------|------|------|
| MAE | 回归 | [0, +∞) | 越小越好 | 预测精度 |
| Accuracy | 分类 | [0, 1] | 越大越好 | 方向判断 |
| MRR | 排序 | [0, K] | 越大越好 | 推荐质量 ⭐ |
| Precision@K | 推荐 | [0, 1] | 越大越好 | Top-K精度 |
| IRR | 收益 | (-∞, +∞) | 越小越好 | 投资回报差距 |

---

## 1️⃣ MAE (Mean Absolute Error)

### 定义
平均绝对误差，衡量预测收益率与实际收益率的偏差。

### 数学公式
```
MAE = (1/n) × Σ|y_pred - y_true|

其中:
  n = 样本数量
  y_pred = 预测的收益率
  y_true = 实际的收益率
```

### 代码实现
```python
# 在train.py中使用PyTorch的L1Loss
reg_loss_func = nn.L1Loss(reduction='none')
reg_loss = reg_loss_func(reg_out, batch_reg_y)

# 评估时使用sklearn
from sklearn.metrics import mean_absolute_error
mae = mean_absolute_error(test_y, y_pred)
```

### 计算示例
```
实际收益率: [0.05, -0.03, 0.02, 0.08, -0.01]
预测收益率: [0.04, -0.02, 0.03, 0.06, -0.02]

绝对误差:   [0.01, 0.01, 0.01, 0.02, 0.01]
MAE = (0.01 + 0.01 + 0.01 + 0.02 + 0.01) / 5 = 0.012
```

### 解读
- **MAE = 0.01**: 平均预测误差1%，非常准确
- **MAE = 0.02**: 平均预测误差2%，表现良好
- **MAE = 0.05**: 平均预测误差5%，需要改进
- **MAE > 0.10**: 平均预测误差10%+，效果较差

### 优缺点
**优点:**
- ✅ 直观易懂
- ✅ 对异常值不敏感（相比MSE）
- ✅ 与目标变量同单位

**缺点:**
- ⚠️ 不考虑误差方向
- ⚠️ 不能反映排序质量

### 可视化建议
1. **训练曲线**: Epoch vs MAE
2. **预测散点图**: 实际值 vs 预测值
3. **误差分布**: 直方图/箱线图
4. **误差热力图**: 不同股票/类别的MAE

---

## 2️⃣ Accuracy (分类准确率)

### 定义
正确预测股票涨跌方向的比例。

### 数学公式
```
Accuracy = (TP + TN) / (TP + TN + FP + FN)

其中:
  TP (True Positive):  预测涨且实际涨
  TN (True Negative):  预测跌且实际跌
  FP (False Positive): 预测涨但实际跌
  FN (False Negative): 预测跌但实际涨
```

### 代码实现
```python
def Acc(test_y, pred_y):
    test_y = np.ravel(test_y)
    pred_y = np.ravel(pred_y)
    # 将收益率转换为涨跌标签
    pred_y = (pred_y > 0) * 1  # >0 为涨(1), ≤0 为跌(0)
    acc_score = sum(test_y == pred_y) / len(pred_y)
    return acc_score
```

### 计算示例
```
实际涨跌: [1, 0, 1, 1, 0]  (1=涨, 0=跌)
预测涨跌: [1, 0, 1, 0, 0]

正确预测: [✓, ✓, ✓, ✗, ✓]
Accuracy = 4/5 = 0.80 (80%)
```

### 解读
- **Acc = 0.50**: 随机猜测水平，无预测能力
- **Acc = 0.60**: 有一定预测能力
- **Acc = 0.70**: 表现良好
- **Acc = 0.80+**: 表现优秀

### 重要性
在股票市场中，**方向准确率**可能比精确的数值预测更重要：
- 做对方向：买入上涨股票，避开下跌股票
- 即使预测幅度有误差，方向对了依然能盈利

### 优缺点
**优点:**
- ✅ 简单直观
- ✅ 符合交易逻辑（做对方向）
- ✅ 易于理解和传达

**缺点:**
- ⚠️ 忽略预测幅度
- ⚠️ 对不平衡数据敏感
- ⚠️ 不能反映推荐质量

### 可视化建议
1. **训练曲线**: Epoch vs Accuracy
2. **混淆矩阵**: TP/TN/FP/FN分布
3. **ROC曲线**: 不同阈值下的性能
4. **类别准确率**: 涨/跌分别的准确率
5. **按类别统计**: 不同股票类别的准确率

---

## 3️⃣ MRR (Mean Reciprocal Rank)

### 定义
平均倒数排名，衡量推荐系统中相关项目在推荐列表中的排名质量。

### 数学公式
```
MRR@K = Σ(1 / rank_i)  for i in Top-K actual

其中:
  rank_i = 第i个真正优秀的股票在预测排名中的位置
  K = 考虑的Top-K数量
```

### 代码实现
```python
def MRR(test_y, pred_y, k=5):
    predict = pd.DataFrame([])
    predict["pred_y"] = pred_y
    predict["y"] = test_y
    
    # 按预测值降序排列，记录预测排名
    predict = predict.sort_values("pred_y", ascending=False).reset_index(drop=True)
    predict["pred_y_rank_index"] = (predict.index) + 1
    
    # 按实际值降序排列，取前K个真正优秀的
    predict = predict.sort_values("y", ascending=False)
    
    # 计算这K个优秀股票在预测排名中的倒数排名之和
    return sum(1 / predict["pred_y_rank_index"][:k])
```

### 计算示例

**场景**: 10支股票，K=3

```
股票    实际收益    预测收益    预测排名
A       0.08       0.07        2      ← 实际Top-3
B       0.06       0.08        1      ← 实际Top-3  
C       0.05       0.04        5      ← 实际Top-3
D       0.03       0.06        3
E       0.02       0.05        4
...

实际Top-3: [A, B, C]
在预测排名中的位置: [2, 1, 5]

MRR@3 = 1/2 + 1/1 + 1/5 = 0.5 + 1.0 + 0.2 = 1.7
```

### 解读
- **MRR@5 = 5.0**: 完美推荐，所有Top-5都排在前5
- **MRR@5 = 3.0-4.0**: 表现优秀
- **MRR@5 = 2.0-3.0**: 表现良好
- **MRR@5 < 1.0**: 表现较差

### 为什么MRR重要？
1. **考虑排序质量**: 不仅要推荐对，还要排序对
2. **符合推荐场景**: 用户更关注前几个推荐
3. **奖励高排名**: 排名越靠前，贡献越大
4. **惩罚低排名**: 好股票排在后面会降低分数

### MRR vs Precision
```
场景: 推荐5支股票

实际Top-5: [A, B, C, D, E]
预测Top-5: [B, F, A, G, C]

Precision@5 = 3/5 = 0.6  (命中3个)
MRR@5 = 1/1 + 1/3 + 1/5 = 1.53

如果调整预测为: [A, B, C, F, G]
Precision@5 = 3/5 = 0.6  (依然是3个)
MRR@5 = 1/1 + 1/2 + 1/3 = 1.83  (更高！)
```

**结论**: MRR对排序敏感，Precision只看命中数量

### 优缺点
**优点:**
- ✅ 考虑排名顺序
- ✅ 符合推荐系统的实际需求
- ✅ 对Top位置更敏感
- ✅ 综合性强

**缺点:**
- ⚠️ 计算相对复杂
- ⚠️ 不够直观
- ⚠️ 依赖K值选择

### 可视化建议
1. **K值影响**: 不同K下的MRR曲线
2. **排名对比**: 预测排名 vs 实际排名的热力图
3. **MRR分解**: 每个股票的贡献（1/rank）
4. **训练曲线**: Epoch vs MRR
5. **股票轨迹**: 每支股票的排名变化

---

## 4️⃣ Precision@K (Top-K精度)

### 定义
推荐的Top-K支股票中，有多少真正表现优秀。

### 数学公式
```
Precision@K = |Predicted_Top_K ∩ Actual_Top_K| / K

其中:
  Predicted_Top_K = 预测的前K支股票
  Actual_Top_K = 实际表现最好的前K支股票
  ∩ = 交集
```

### 代码实现
```python
def Precision(test_y, pred_y, k=5):
    predict = pd.DataFrame([])
    predict["pred_y"] = pred_y
    predict["y"] = test_y
    
    # 预测Top-K
    predict1 = predict.sort_values("pred_y", ascending=False)
    # 实际Top-K
    predict2 = predict.sort_values("y", ascending=False)
    
    # 计算交集
    correct = len(list(
        set(predict1["y"][:k].index) & 
        set(predict2["y"][:k].index)
    ))
    
    return correct / k
```

### 计算示例

**场景**: 10支股票，K=5

```
股票    实际收益    预测收益
A       0.08       0.07
B       0.06       0.08
C       0.05       0.04
D       0.03       0.06
E       0.02       0.05
F       0.01       0.03
G      -0.01       0.02
H      -0.02       0.01
I      -0.03      -0.01
J      -0.05      -0.02

预测Top-5: [B, D, A, E, C]
实际Top-5: [A, B, C, D, E]

交集: {A, B, C, D, E} = 5个
Precision@5 = 5/5 = 1.0 (完美)
```

### 不同K值的意义

**Precision@5**: 
- 适合小额投资者
- 关注最优质的少数股票
- 要求更高的精度

**Precision@10**:
- 适合中等投资者
- 平衡风险和收益
- 更实用的指标

**Precision@20**:
- 适合大额投资者或机构
- 构建多样化投资组合
- 更宽松的标准

### 实际意义

**交易场景**:
```
假设有100万资金，想投资5支股票

Precision@5 = 0.8 意味着:
- 推荐5支，其中4支真正优秀
- 80%的资金投在正确的方向
- 只有20%的资金可能亏损
```

### 优缺点
**优点:**
- ✅ 非常直观
- ✅ 直接对应业务目标
- ✅ 易于理解和传达
- ✅ 适合不同场景（调整K）

**缺点:**
- ⚠️ 不考虑排序顺序
- ⚠️ 阈值选择（K）影响大
- ⚠️ 忽略具体收益差距

### 可视化建议
1. **K值对比**: K=5/10/20的柱状图
2. **韦恩图**: 预测集和实际集的重叠
3. **命中矩阵**: 每支股票是否命中
4. **训练曲线**: Epoch vs Precision
5. **堆叠图**: 命中/未命中的比例
6. **类别分析**: 不同股票类别的精度

---

## 5️⃣ IRR (Investment Return Rate)

### 定义
理想投资组合与实际推荐组合的收益差距。

### 数学公式
```
IRR@K = Σ(Actual_Top_K_Returns) - Σ(Predicted_Top_K_Returns)

理想情况: IRR = 0 (完美预测)
```

### 代码实现
```python
def IRR(test_y, pred_y, k=5):
    predict = pd.DataFrame([])
    predict["pred_y"] = pred_y
    predict["y"] = test_y
    
    predict1 = predict.sort_values("pred_y", ascending=False)
    predict2 = predict.sort_values("y", ascending=False)
    
    # 理想收益 - 实际推荐收益
    return sum(predict2["y"][:k]) - sum(predict1["y"][:k])
```

### 计算示例

**场景**: K=3

```
股票    实际收益    预测排名
A       0.08       2
B       0.06       1
C       0.05       5
D       0.03       3
E       0.02       4

预测Top-3: [B, A, D]
实际Top-3: [A, B, C]

预测组合收益: 0.06 + 0.08 + 0.03 = 0.17
理想组合收益: 0.08 + 0.06 + 0.05 = 0.19

IRR@3 = 0.19 - 0.17 = 0.02
```

### 解读
- **IRR = 0**: 完美推荐，收益最大化
- **IRR > 0**: 推荐不够好，损失了收益
- **IRR < 0**: （理论上不应出现，说明评估有问题）

### 实际意义

**投资场景**:
```
假设投资100万，平均分配给Top-5

IRR@5 = 0.05 意味着:
- 理想收益: 5万
- 实际收益: 0万
- 损失: 5万 (占投资的5%)
```

### 为什么代码中未使用？
在train.py中定义了IRR函数但未在主循环中计算，原因可能是：
1. MRR已经能很好地评估排序质量
2. IRR计算成本较高
3. IRR与Precision/MRR高度相关

### 优缺点
**优点:**
- ✅ 直接反映收益损失
- ✅ 有明确的经济意义
- ✅ 易于向业务方解释

**缺点:**
- ⚠️ 受收益率分布影响大
- ⚠️ 与MRR高度相关
- ⚠️ 可能不够稳定

### 可视化建议
1. **收益对比**: 理想 vs 实际的柱状图
2. **损失瀑布图**: 展示每个错误推荐的损失
3. **K值影响**: 不同K下的IRR
4. **累积收益**: 训练过程中的累积损失

---

## 🎯 指标之间的关系

### 相关性分析

```
MAE ←→ Accuracy: 中等相关
- MAE小 ≈ 方向也容易对
- 但MAE=0.01可能Acc=0.6或0.8

Accuracy ←→ MRR: 弱相关  
- 方向对不等于排序对
- Acc=0.7可能MRR很低（顺序错）

MRR ←→ Precision@K: 强相关
- MRR高 → Precision高
- 但MRR更关注排序，Precision只看命中

MRR ←→ IRR: 强相关
- MRR高 → IRR低（损失小）
- IRR直接反映经济损失
```

### 指标权重建议

**回归为主的场景**:
```
MAE: 40%
Accuracy: 30%  
MRR: 20%
Precision: 10%
```

**推荐为主的场景** (FinGAT的目标):
```
MRR: 40%      ← 主要指标
Precision: 30%
Accuracy: 20%
MAE: 10%
```

### 指标组合策略

**保守策略**: 重视精度
- 高MAE权重
- 高Accuracy权重
- 适合: 风险厌恶型投资者

**激进策略**: 重视排序
- 高MRR权重
- 高Precision@5权重  
- 适合: 追求高收益的投资者

**平衡策略**: 全面考虑
- 所有指标均衡权重
- 适合: 大多数场景

---

## 📊 可视化设计原则

### 1. 时序类指标 (MAE, Accuracy, MRR)
- **主图**: 折线图，展示训练过程
- **辅图**: 平滑曲线，去除噪声
- **标注**: 最佳点、趋势线

### 2. 对比类指标 (Precision@K)
- **主图**: 柱状图/雷达图
- **辅图**: 堆叠图/热力图
- **标注**: 最佳K值、差异百分比

### 3. 分布类指标 (误差分布)
- **主图**: 直方图/箱线图
- **辅图**: 小提琴图/密度图
- **标注**: 均值、中位数、异常值

### 4. 关系类指标 (预测vs实际)
- **主图**: 散点图/相关图
- **辅图**: 残差图/Q-Q图
- **标注**: 拟合线、置信区间

### 5. 综合仪表盘
- **多子图布局**
- **交互式筛选**
- **导出功能**

---

## 🎨 颜色编码建议

```
MAE:        蓝色系 (#1f77b4) - 理性、精确
Accuracy:   绿色系 (#2ca02c) - 正确、成功
MRR:        橙色系 (#ff7f0e) - 重要、关注
Precision:  紫色系 (#9467bd) - 高端、精英
IRR:        红色系 (#d62728) - 收益、损失

训练集:     实线
测试集:     虚线
最佳点:     星号标记
```

---

## 📈 评价标准参考

### 优秀模型标准
```
MAE < 0.020
Accuracy > 0.65
MRR@5 > 0.75
Precision@5 > 0.70
```

### 良好模型标准
```
MAE < 0.030
Accuracy > 0.60
MRR@5 > 0.70
Precision@5 > 0.60
```

### 基线模型标准
```
MAE < 0.050
Accuracy > 0.55
MRR@5 > 0.60
Precision@5 > 0.50
```

---

## 💡 实用建议

### 1. 开发阶段
- 主要关注: **MAE** 和 **Accuracy**
- 快速验证模型是否学到东西

### 2. 调优阶段  
- 主要关注: **MRR** 和 **Precision@K**
- 优化排序和推荐质量

### 3. 部署阶段
- 综合评估: **所有指标**
- 根据业务需求加权

### 4. 监控阶段
- 实时跟踪: **MRR** (主要) + **Accuracy** (辅助)
- 定期回测: **IRR** (经济效益)

---

**总结**: 这5个指标从不同角度评估模型，MAE关注预测精度，Accuracy关注方向判断，MRR和Precision关注推荐质量，IRR关注经济效益。在实际应用中，应根据业务目标合理选择和权衡这些指标。
